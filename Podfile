# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'Pitch Shifter' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Pitch Shifter
  pod 'AudioKit'
  pod 'EZAudio'
  pod 'TPCircularBuffer'
  pod 'AudioKit/UI'
end
